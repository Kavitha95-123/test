/*import { expect, test } from "@playwright/test";
test("has title",async({page})=>{
    await page.goto("https://www.amazon.in/");
    await expect(page).toHaveTitle("Online Shopping site in India: Shop Online for Mobiles, Books, Watches, Shoes and More - Amazon.in")
});

//import {expect, test } from "@playwright/test";
test.fixme("has lipstick",async({page})=>{
    await page.goto("https://www.flipkart.com/");
    await page.getByPlaceholder("Search for Products, Brands and More").fill("mobile");
    await page.keyboard.press("Enter");
});

import{chromium,expect,test, webkit}from '@playwright/test';
(aync () => {
    let browser = await webkit.launch();
    let context = await browser.newContext();
    let page = await context.newPage();
    await page.goto("https:example.com");
    await page.screenshot({path: 'screenshot.png'});
    await browser.close();
})();

import { expect, test } from "@playwright/test";
import { console } from "inspector";
test("has title",async({page})=>{
    await page.goto("https://demoapps.qspiders.com/");
    //await page.waitForTimeout(4000)
    //await page.getByPlaceholder("Enter Name").fill("kavi");
    //await page.waitForTimeout(4000)
    //await page.locator('#email').fill("kavithasriram59@gmail.com")
    await expect(page.getByTitle('Issues Count')).toHaveText('25 issues');
    await page.locator('#btn').click()
});

*/






 






















